<table>
        <tr><th>Data</th><th>Co wylosowano</th></tr>
    {% for rollDice in rollDices %}
            <tr>
                <td>{{rollDice.time.format('Y-m-d H:i')}}</td>
                <td>
                    {% for diceSide in rollDice.diceSides %}
                        [{{diceSide.animalType.name}}]
                    {% endfor %}
                </td>
            </tr>
    {% endfor %}
  </table>